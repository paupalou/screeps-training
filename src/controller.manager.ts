import { log } from "./utils";

export class ControllerManager {
    static work(room: Room) {
        // if (ControllerManager.needSpawnUpgrader(room)) {
        //     ControllerManager.spawnUpgrader(room);
        // }
        //
        // _.forEach(ControllerManager.upgraders(room), upgrader => {
        //     Upgrader.work(upgrader);
        // });
    }

    static upgraderPositions(room: Room) {
      log("batum")
    }
}
